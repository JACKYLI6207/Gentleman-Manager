---
description: 改動 src 或 src-tauri 後自動建置 Gentleman-Manager.exe / ZIP，無需使用者提醒
alwaysApply: true
---

# 自動建置 EXE/ZIP

在此專案完成會影響應用程式的實作後（含 `src/`、`src-tauri/`、建置腳本），**必須自行執行 EXE 建置**，不要等使用者開口。

## 建置方式

在專案根目錄執行：

```powershell
powershell -ExecutionPolicy Bypass -File "skill\scripts\rebuild-exe.ps1"
```

## 時機

- 前端、Rust、或兩者皆有修改且任務視為完成時
- 僅回答問題、純 code review、或未改程式碼時不必建置

## 回報

完成後簡短告知產物路徑：`Gentleman-Manager.exe` 與 `Gentleman-Manager.zip`（若舊 EXE 佔用中，腳本會輸出 `Gentleman-Manager-new.exe`）。
